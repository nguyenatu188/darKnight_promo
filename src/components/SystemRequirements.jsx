import React from 'react'
import { SectionWrapper } from '../hoc'

const SystemRequirements = () => {
  return (
    <div>
      System Requirements
    </div>
  )
}

export default SectionWrapper(SystemRequirements, 'systemRequirement')
